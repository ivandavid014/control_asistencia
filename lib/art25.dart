import 'package:flutter/material.dart';

class Art25 extends StatelessWidget {
  const Art25({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Art 25'),
      ),
    );
  }
}
